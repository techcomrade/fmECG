export const APP_HOST = process.env.APP_HOST || '171.244.8.114'
export const APP_PORT = process.env.APP_PORT || 80;
export const API_URL = `http://${APP_HOST}:${APP_PORT}/api`;
export const JWT_TOKEN = 'token';